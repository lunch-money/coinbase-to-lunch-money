/**
 * Main Spec
 */
describe('main', () => {
  /**
   * Initate Spec
   */
  describe('initiate', () => {
    it('should have tests', async () => {
      console.log('todo');
    });
  });

  /**
   * Get Balances Spec
   */
  describe('getBalances', () => {
    it('should have tests', async () => {
      console.log('todo');
    });
  });
});
